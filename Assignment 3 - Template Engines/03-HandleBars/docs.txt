1. 
Install npm and nodemon 
npm init
npm install --save-dev nodemon

2.
EDIT SCRIPT IN package.json
 "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },

  add start
   "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"nodemon app.js"
  },
3. Install Express
   npm install --save express

4. Create the basic front end
   Put Html files in views and css files in public


5. Create app.js
    
6. Installing pug express-handlebars and EJS 
 npm install --save ejs pug express-handlebars    
